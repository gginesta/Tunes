# Spotify API client ID (used by the frontend)
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Server port (optional, defaults to 3000)
PORT=3000

# Node environment (set to "production" for deployment)
NODE_ENV=production
